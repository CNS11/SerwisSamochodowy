@model IEnumerable<WebApplication1.Models.Modele>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Marki.NazwaMarki)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NazwaModelu)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Marki.NazwaMarki)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NazwaModelu)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdModelu }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdModelu }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdModelu })
        </td>
    </tr>
}

</table>
